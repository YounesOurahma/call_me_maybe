[project]
name = "call-me-maybe"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.0",
    "pydantic",
    "flake8",
    "mypy",
    "torch>=2.12.1",
    "transformers>=5.12.1",
]

[tool.mypy]
follow_imports = "skip"
